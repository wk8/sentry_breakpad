require 'spec_helper'

describe SentryBreakpad do
  it 'has a version number' do
    expect(SentryBreakpad::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
